name: Deplot to server
on:
  push:
    branches: ["main"]

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello World
        run: echo "Hello World"

  second_job:
    needs: first_job
    runs-on: ubuntu-latest
    steps:
      - name: Another Step
        run: echo "This runs after first_job"
