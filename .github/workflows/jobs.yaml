name: deploy
on: 
  push:
    branches: ["main"]

jobs:
    first-job:
        runs-on: ubuntu-latest

        env: 
         NAME: Codigofacilito

        steps:
          - name: Hello World
            run: "Hello World, $NAME"

    second-job:
        needs: first-job
        runs-on: ubuntu-latest
        steps:
            - name: Hello World
              run: echo "Hello World, ${{ github.ref }}"
